/**Dada una cadena de text y un numero, recortar el string, mostrando los X primeros caracteres
 * 
 * ejemplo: recortar(Cursos Desarrollo web, 6) //Devuelve 'Cursos'
 * 
 * -crear una funcion con parametros text y hasta
 * -comprobar que los datos son correctos
 * -recortar string con sub strng
 * -Devolver el resultado
 * 
 */

function recortar(texto, numero){
    let resultado = "";

    if(typeof texto === 'string' && typeof numero === 'number'){

    }else{
        resultado = "Introduzca los datos de manera correcta";
    }
}

console.log(recortar("Cursos de desarrollo", 6));
