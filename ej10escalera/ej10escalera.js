/* Generar una escalara donde el numero de escalones este determinado 
por un numero parametro [-] */

function pintarEscalera(numero){
    let = escalera_completa = "";

    for(let nivel = 1; nivel <= numero; nivel++){

        let escalones = "";
        for(let escalon = 1; escalon <= nivel; escalon++){
            escalones += "[-]"
        }
    }
}

escalera(5)