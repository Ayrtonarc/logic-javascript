/**
 * Calcular el X porcentaje de un numero X
 */

function calcularPorcentajes(porcentaje, numero){
    let operacion = (numero * (porcentaje/100))
    let resultado = `El ${porcentaje} % de ${numero} es ${operacion}`
}